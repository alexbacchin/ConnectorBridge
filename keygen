#!/usr/bin/perl

use strict;
use warnings;
use Crypt::Mode::ECB;

my $token = '--TOKEN FROM DISCOVER--';
my $key = '--KEY FROM APP--';

my $ecb = Crypt::Mode::ECB->new('AES');
my $ciphertext =$ecb->encrypt($token, $key);
$ciphertext =~ s/(.)/sprintf("%x",ord($1))/eg;
print substr(uc($ciphertext),0,32),"\n";

